/** Clave IndexedDB para conversaciones (idb-keyval). Usada también para borrado completo (derecho de supresión RGPD). */
export const STORAGE_KEY_CONVERSATIONS = 'lexia_conversations';

/** Prefijo para borradores por conversación en localStorage. Clave final: lexia_draft_<conversationId> */
export const STORAGE_KEY_DRAFT_PREFIX = 'lexia_draft_';
